-- Optional dev seed (run after migrations)
-- Creates a demo org + center + admin user (password: Admin@123)

-- Password hash for "Admin@123" (bcrypt) — regenerate in production
-- Use: python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt']).hash('Admin@123'))"
